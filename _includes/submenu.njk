{% if (menu == "true") and (childPages.length > 0) %}
    <dl>
        {%if name != "Journal" %}
            {% for child in childPages %}
                {% if child.priv == "false" %}
                    <dt>
                        <a href="{{ child.slug }}.html">
                            {{ child.name }}
                        </a>
                    </dt>
                    <dd>
                        {{child.bref}}
                    </dd>
                {% endif %}
            {% endfor %}
        {% else %}
            {% for child in childPages|sort(false, true, 'date')|reverse %}
                {% if child.priv == "false" %}
                    <dt>
                        <a href="{{ child.slug }}.html">
                            {{ child.name }}
                        </a>
                    </dt>
                    <dd>
                        {{child.bref}}
                    </dd>
                {% endif %}
            {% endfor %}
        {% endif %}
    </dl>
{% endif %}